machine:
  services:
    - docker
dependencies:
  override:
    - git submodule update --init
    - docker build -t test .
test:
  override:
    - docker run -ti test /bin/true
    - docker run -ti test /bin/bash --version
